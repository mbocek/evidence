For building you will need https://github.com/panter/org.vaadin.mvp.
Download source code and build it with command:
> mvn clean install

For building project you can use:
> mvn clean install jetty:run

For remote debugging:
> mvnDebug clean install jetty:run